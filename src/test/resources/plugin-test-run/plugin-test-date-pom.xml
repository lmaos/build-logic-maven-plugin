<project>
    <build>
        <plugins>
            <plugin>
                <groupId>com.clmcat.maven.plugins</groupId>
                <artifactId>build-logic-maven-plugin</artifactId>
                <version>1.0-SNAPSHOT</version>
                <configuration>
                    <main>
                        <date name="date" format="yyyyMMddHHmmss"/>
                        <echo>${date}</echo>
                    </main>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>