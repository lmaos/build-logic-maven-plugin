<project>
    <build>
        <plugins>
            <plugin>
                <groupId>com.clmcat.maven.plugins</groupId>
                <artifactId>build-logic-maven-plugin</artifactId>
                <version>1.0-SNAPSHOT</version>
                <configuration>
                    <main>
                        <file name="unsafeFile" path="${project.basedir}/../unsafe-output.txt" />
                        <write file="unsafeFile" overwrite="true">blocked</write>
                    </main>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
