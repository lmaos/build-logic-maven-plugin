<project>
    <build>
        <plugins>
            <plugin>
                <groupId>com.clmcat.maven.plugins</groupId>
                <artifactId>build-logic-maven-plugin</artifactId>
                <version>1.0-SNAPSHOT</version>
                <configuration>
                    <main>
                        <foreach collection="missingItems" item="item">
                            <echo>${item}</echo>
                        </foreach>
                    </main>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
